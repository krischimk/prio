/**
 * Platzhalter.
 *
 * Die eigentliche Anwendung (Anmeldung, Listen, Aufgaben, Sync-Anzeige) folgt in
 * einem eigenen Schritt – so bleibt jeder Commit für sich lauffähig.
 */
export function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-neutral-950 text-neutral-100">
      prio
    </div>
  )
}
